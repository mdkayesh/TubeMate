// import { reactive } from "vue";
// import { User } from "@/types";

// type Store = {
//   user: User | null;
// };

// const store: Store = reactive({
//   user:
//     (JSON.parse(localStorage.getItem("userinfo") as string) as User) || null,
// });

// export default store;
